# 多模态模板与版式变体

本文说明 v0.2.0 新增的两块能力：**多模态提示词模板** 与 **离线版式变体引擎**。

---

## 一、多模态提示词模板

### 定位

产出的仍然是**提示词**，不调用任何生成 API。
目的是把用户一句模糊的想法，整理成可以直接粘贴到目标生成工具的高质量提示词。

### 新增框架（4 个）

| key | 名称 | 字段 | 目标工具（举例） |
|---|---|---|---|
| `image` | IMAGE | Subject 主体 / Style 风格 / Composition 构图 / Lighting 光照 / Details 细节 / Params 参数 | Midjourney、Stable Diffusion、Flux、即梦、可图 |
| `voice` | VOICE | Script 文案 / Voice 音色 / Emotion 情绪 / Pacing 节奏 / Direction 演绎指导 / Format 交付格式 | ElevenLabs、火山语音、Azure TTS、CosyVoice |
| `video` | VIDEO | Shot 镜头 / Subject 主体与动作 / Camera 运镜 / Duration 时长 / Transition 转场 / Audio 声音 | 可灵、Runway、Sora、即梦、Veo |
| `music` | MUSIC | Genre 风格 / Instrumentation 配器 / Mood 情绪 / Tempo 速度与节拍 / Structure 结构 / Production 制作质感 | Suno、Udio、Stable Audio |

### 设计要点

1. **输出的是提示词，不是解释。** 每个框架的 `buildPrompt` 都明确要求「直接输出可直接粘贴使用的提示词，不要输出框架分析、要素清单或解释说明」。
2. **保留领域惯用术语。** 镜头语言（`推/拉/摇/移/跟拍/环绕`）、录影参数（`--ar 16:9 --stylize 250`）、音乐标记（`[Verse] / [Chorus] / [Bridge]`、`BPM`、调性）按行业惯例书写。
3. **语言策略。** 主体描述跟随用户语言；通用风格/技术关键词保留英文（生成模型对英文术语识别更准）。
4. **不编造。** 明确禁止编造原想法中不存在的品牌、真人姓名、数据。
5. **可交付性。** VOICE 要求给出**可直接朗读的完整文案全文**（而非提纲），停顿/重音用 `（停顿 0.5 秒）` 内联标注，便于 TTS 解析；MUSIC 要求给出 BPM 与调性建议，歌词按段落标记写全。

### 模板库（新增 15 个，覆盖 4 个新分类）

| 分类 | 模板 |
|---|---|
| 图像生成（4） | 产品主图 / 人像写真 / 场景概念图 / Logo与图标 |
| 配音语音（4） | 纪录片旁白 / 广告口播 / 有声书分角色 / 课程讲解 |
| 视频生成（4） | 短视频分镜 / 产品演示视频 / 音乐短片MV / 动画分镜 |
| 音乐生成（3） | 视频配乐 / 原创歌曲 / 品牌主题曲 |

> 每个模板都带完整的示例 `idea` 与使用提示 `tip`，不为占位符。

### 当前总量

**7 个框架 / 29 个模板 / 11 个分类**
（原 3 个文本框架 16 个模板 + 新增 4 个多模态框架 15 个模板）

---

## 二、离线版式变体引擎

### 解决的问题

不配置大模型时，本地规则引擎会把任何输入都渲染成同一套
`## 角色 / ## 背景与上下文 / ## 任务 / ## 约束与参数 / ## 输出格式`，
换十个模板看起来也像同一个人写的。

### 方案

`js/style-variants.js` 把**同一份结构化内容**渲染成 6 种版式。
版式只改变「怎么排布、用什么标记、详略程度」，**不改变内容本身**。

| key | 名称 | 排布方式 | 适合场景 |
|---|---|---|---|
| `standard` | 标准分节 | `## 标题` + 分节，`\n\n` 分段 | 正式交付、需要归档 |
| `minimal` | 极简指令 | 压缩成一段话，多行内容用 `；` 压平，无标题 | 直接粘贴进对话框 |
| `roleplay` | 角色扮演 | 第二人称，「你现在的身份：」「现在，请完成这件事：」，正文缩进两格 | persona、人格化场景 |
| `stepwise` | 逐步引导 | `1. **执行核心任务**` 编号步骤，正文缩进三格 | 流程复杂、易漏项 |
| `annotated` | 带示例 | 标题后补「（例如：…）」，减少跑偏 | 用户没说清、模型易误解 |
| `checklist` | 清单核对 | `- [ ] **角色定位**` + 缩进子项 | 逐条确认与验收 |

### 用法

```js
StyleVariants.getStyles()                     // 元数据数组（供 UI 渲染）
StyleVariants.isKnownStyle('minimal')         // → true
StyleVariants.render(sections, 'checklist', 'zh');
```

`render(sections, styleKey, lang)`

- `sections`：`{ role, context, task, params, format }`，每项是 `string[]`（可为空数组）
- `styleKey`：见上表；未知值回落到 `standard`
- `lang`：`'zh'` | `'en'`

### 「自动挑选」

`PromptOptimizer.pickStyle(requested, src, report)` 负责选版式。判定顺序是
**先看用户显式表达的意图，再看篇幅**：

1. 说了「按步骤 / 先…再…最后」→ `stepwise`
2. 已经在用列表列参数 → `checklist`（长）或 `annotated`（短）
3. 指定了角色身份（`你是一位…` / `作为…` / `扮演…` / `以…身份`）→ `roleplay`
4. 明确要求逐项核对 → `checklist`
5. 再看篇幅与信息密度：
   - `< 14` 字且维度几乎全缺 → `minimal`
   - `< 30` 字 → 按请求性质分：改写/翻译类 `annotated`，对比/解释类 `stepwise`，其余 `annotated`
   - `≥ 30` 字且缺失 ≥3 维 → `annotated`
   - `> 300` 字且结构完整 → `checklist`
   - `> 180` 字 → `stepwise`
   - 其余 → `annotated`

> **为什么长度判定要放在结构信号之后？**
> 早期版本把「长度 < 40 → 极简」放在最前面，结果「第一步…然后…最后」这种
> 明确要求分步的短输入也被压成一段话，正好搞反了用户意图。
> 定序原则见 `js/optimizer.js` 中 `pickStyle` 上方的注释。

### UI

- 「提示词优化」视图 → **版式风格** 下拉：`自动挑选` + 6 种
- 结果区顶部有**版式胶囊按钮**，可对同一份输入即时切换对比（仅本地规则引擎）
- 优化报告行会显示本次实际使用的版式

---

## 三、已知取舍

| 项 | 说明 |
|---|---|
| 版式只作用于本地规则引擎 | AI 深度优化的输出由模型自行组织，版式切换对该引擎无效 |
| `minimal` 仍是最常见结果 | 真实使用中大量输入是一句话短请求，这类输入压成一段话确实是更优解；已通过「按请求性质分流」降低其占比（实测 33%） |
| 多模态不接生成 API | 按需求只做提示词模板；如需接入生成，需另加服务端与鉴权层 |
| 单句输入不做归类 | 唯一例外是整句即为角色指派（如「你是 Python 专家」），此时归到角色 |

---

## 四、回归防护

`.workbuddy/` 下有三个校验脚本，修改这两块功能后应当重跑：

| 脚本 | 覆盖 |
|---|---|
| `check-frameworks.mjs` | 7 框架 / 29 模板完整性、无重复 id、无占位符残留、多模态分类覆盖、`buildPrompt` 冒烟、**structuredData 三种形态兼容** |
| `check-styles.mjs` | 6 风格 × 2 语言的渲染、内容零丢失、互异性、边界（未知风格/全空/只有 task） |
| `check-ui-wiring.mjs` | `index.html` 控件 id 与 `optEl()` 引用一一对应、脚本加载顺序、`sw.js` 预缓存一致性、最小 DOM 下的完整交互冒烟 |
| `check-distribution.mjs` | 33 条真实语料下「自动挑选」的版式分布，防止塌缩回单一版式 |
