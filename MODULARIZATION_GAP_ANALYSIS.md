# 模块化迁移前：与成熟商业化软件的差距分析

> 分析基准：当前 `main`（提交 `7f6eb88`），代码规模 `js/` 约 3600 行 / 17 个脚本，
> `css/style.css` 575 行，`index.html` 649 行，`src-tauri/src/lib.rs` 约 890 行。
> 所有结论均基于实际代码扫描（文件 + 行号），不做泛泛而谈。
> 商业化产品的"典型做法"指同类桌面/Web 混合工具（如 Raycast、Obsidian、Notion 客户端、VS Code）的通行水位。

---

## 执行摘要

**当前定位**：功能完整度已达"可用产品"，工程质量（校验/构建/发布）明显高于同类个人项目——
454 项自动化断言、发布链路有防呆、CI 已跑通。但**架构形态仍停留在"单页脚本集合"阶段**，
**UI/UX 停留在"能用的原型"阶段**。

**一句话结论**：
> 最大的差距不在功能，而在**可访问性与交互一致性的系统性缺失**，以及**无模块边界导致任何改动都是全量回归**。
> 模块化迁移应优先解决"边界"，UI 建设应优先解决"键盘/读屏/反馈"这三件最低成本、最高感知的事。

**差距速览**（⭐ 越多差距越大）

| 维度 | 差距 | 最关键的三个具体问题 |
|---|---|---|
| 1 架构与模块化 | ⭐⭐⭐⭐⭐ | 无打包器/无模块边界；`app.js` 1814 行承担 9 类职责；删一个文件要改 4 处 |
| 2 界面交互与体验 | ⭐⭐⭐⭐⭐ | 全项目仅 1 个 `aria-*`、0 个 `:focus-visible`；4 处原生 `confirm()`；无全局错误兜底 |
| 3 性能与稳定性 | ⭐⭐⭐ | 0 处防抖；0 懒加载；无错误上报（生产不可观测） |
| 4 配置化与国际化 | ⭐⭐⭐⭐ | 794 处硬编码中文，无 i18n 层；个性化仅覆盖颜色/字体/背景 |
| 5 测试与文档 | ⭐⭐⭐ | 自制沙盒（非真实浏览器）；8 篇文档全是审查/测试报告，缺用户手册与架构文档 |

---

## 维度一：架构与模块化程度

### 1.1 现状

| 项 | 实测 |
|---|---|
| 模块机制 | **无**。`index.html` 里 17 个 `<script src>` 顺序加载，靠顶层 `const`/`function` 的全局词法绑定耦合 |
| 依赖声明 | 隐式。`index.html` 的标签顺序 = 唯一权威依赖声明 |
| 单文件规模 | `js/app.js` **1814 行 / 70KB**；`js/frameworks.js` 432 行；`js/optimizer.js` 533 行 |
| 跨文件访问 | 顶层 `const` 不挂 `window`，需显式 `window.X = X`（`offline-llm.js:381`、`style-variants.js:252`） |
| 已分离的模块 | `optimizer.js` / `optimize-ui.js` / `style-variants.js` / `personalize.js` / `export.js` / `sync.js` 已具备模块雏形 |

### 1.2 具体短板

**S1-1 无模块边界 → 任何改动都是全量回归**
`app.js` 同时承担：全局状态、优化主流程、评分、框架对比、分享、历史、模板、设置、更新检查（9 类职责）。
商业化产品会把这些拆成 `features/optimize`、`features/compare`、`features/settings` 等垂直切片，
每个切片自带状态与 UI，改动互不影响。当前你改"分享链接长度"要在一个 1814 行的文件里定位（我这次就在第 486 行改的）。

**S1-2 依赖靠"顺序 + 命名约定"，删文件要改 4 处**
- `index.html` 的 `<script>`
- `sw.js` 的 `STATIC_CACHE_URLS`
- `sw.js` 的 `CACHE_NAME`（否则老客户端用旧清单 → 404 → SW 安装失败）
- 各处的裸引用

这是本项目**最容易出事**的地方：历史已发生"删了 `model-manager.js` 后 SW 缓存仍指向它"。
商业化项目用打包器/静态分析，依赖关系是自动推导的，删文件不会有隐藏引用。

**S1-3 UI 与逻辑混写，无法单测**
`app.js` 里 `switchView()` / `renderHistory()` 等函数直接 `document.getElementById(...)` + `innerHTML`。
商业化产品会用"组件 = 状态 → 渲染函数"的结构，渲染层可独立测试。

**S1-4 状态是裸全局对象**
`const state = {...}`（`app.js:8`）由散落各处的函数直接改写，没有订阅/派发机制，
`state._autoReason` 甚至未在字面量里声明（`app.js:631` 才首次赋值）——典型的"字段蔓延"。

### 1.3 与商业化产品的差距

| | 当前 | 商业化典型 |
|---|---|---|
| 模块边界 | 无，全局词法作用域 | ESM/打包器，显式 import/export |
| 依赖管理 | 手工维护顺序 + 4 处同步 | 自动推导，循环依赖会被静态检查拦下 |
| 可扩展性 | 新增功能 = 往 `app.js` 加函数 | 新增 = 加一个 feature 目录 |
| 类型安全 | 无 | TypeScript 或至少 JSDoc + `checkJs` |

### 1.4 模块化迁移中如何解决

1. **先立边界，再搬代码**：按"领域"划分目录（`core/` `features/` `ui/` `data/`），
   先定义每个目录的**公开出口**（`index.js`），再逐步把 `app.js` 的函数搬进去。
   **不要**一上来就改 `<script type="module">`——17 个文件的加载顺序一变，整站就崩。
2. **迁移期用"适配层"过渡**：先让 `app.js` 变成"只做装配"的壳，
   内部调用各 feature 的公开 API；确认稳定后再切 ESM。
3. **把"删文件 4 处同步"自动化**：现有 `check-ui-wiring.mjs` 已经在校验
   `index.html` ↔ `sw.js` 的一致性，迁移后应改为从打包产物自动生成 SW 清单，从根上消除手工同步。
4. **状态管理后置**：先把 UI 与逻辑分离，再引入极简 store（订阅/派发即可，不必上 Redux）。

---

## 维度二：界面交互与用户体验（重点）

### 2.1 视觉一致性

**已有基础（值得肯定）**
- `css/style.css` 定义了 **38 个 CSS 自定义属性**（设计令牌），6 个 `@keyframes`，24 处 `transition`，10 个 `@media` —— 说明已有设计体系意识。
- 95 个语义化 `<button>`，**0 个 `div onclick`** —— 语义基础扎实，这点比很多项目好。

**短板**

**S2-1 弹窗两套风格（最刺眼的不一致）**
自建了 `askDialog()`（`app.js:1139`）用来替代原生弹窗，被调用 6 次；
但仍有 **4 处直接用原生 `confirm()`**：

| 文件:行 | 场景 |
|---|---|
| `app.js:945` | 清空全部历史记录 |
| `export.js:78` | 导入覆盖确认 |
| `local-model.js:486` | 删除模型缓存 |
| `offline-ui.js:182` | 清除模型缓存 |

后果：一半弹窗是应用风格（可定制文案/按钮/危险色），一半是浏览器原生——
在 Tauri WebView 里原生弹窗尤其突兀，且**无法国际化、无法加危险色、无法做无障碍**。
商业化产品 100% 统一自绘弹窗。

**S2-2 内联样式绕过设计令牌**
`index.html` 与 JS 模板字符串里大量 `style="..."`（如 `app.js:1147` 的 `text-align:left;font-size:.9rem;...`，
设置页约 30 处内联 style）。令牌体系被绕过，改主题时这些地方不会跟着变。

**S2-3 Toast 无状态区分**
`toast()`（`app.js:1086`）只有一个固定样式，成功/失败/警告长得一样，
用户无法从颜色判断操作结果。商业化产品至少有 `success / error / warning / info` 四态 + 图标。

### 2.2 操作反馈

**S2-4 Toast 能力过弱**
- 单条覆盖：连续两次操作只看到最后一条
- 固定 2500ms：长文案来不及读
- 无队列、无操作按钮（不能"撤销"）
- 无 `aria-live`：读屏软件完全感知不到

**S2-5 空状态几乎缺失**
全项目**只有一处**空状态：`history-empty`（`app.js:907`，"暂无历史记录"）。
以下场景均无空状态或提示：
- 模板库按分类筛选无结果
- 框架对比未输入内容时点击
- 设置页离线模型未下载时的模型卡片区
- 更新检查失败时的帮助页

商业化产品会为**每一个可为空的容器**设计空状态（含插画 + 说明 + 主行动按钮）。

**S2-6 加载态覆盖不全**
只有：文本提示（`loading-text`，4 处文案切换）+ 框架对比骨架屏（`app.js:289 renderCompareSkeleton`）。
历史列表、模板网格、设置页加载均无骨架/占位，切换视图时会闪白。
另外**无进度确定性**：离线模型下载有进度条，但生成提示词只有无限动画。

### 2.3 动效

**S2-7 无 `prefers-reduced-motion`（0 处）**
项目有 6 个 `@keyframes` + 24 处 `transition`，但**完全没有**响应系统的"减少动态效果"设置。
对前庭功能敏感的用户是明确的健康风险，也是 WCAG 2.3.3 的要求。
商业化产品（GitHub、Notion、VS Code）都做了这个适配，成本极低（一段 `@media`）。

**S2-8 动效缺少语义**
现有动效多为装饰性（orb 背景、按钮 hover）。
缺少"状态转移动效"：如生成完成→结果区的出现、删除项→列表收缩、错误→抖动提示。

### 2.4 异常与空状态处理

**S2-9 无全局错误兜底（最危险的稳定性缺口）**
扫描结果：
- `window.onerror` / `error` 监听：**0**
- `unhandledrejection`：**0**
- 错误边界：**0**

含义：任何未捕获的异常 = **静默失败或白屏**，且用户无法反馈、你无法得知。
商业化产品标配：全局错误捕获 + 友好降级 UI + 错误上报（Sentry 类）+ 用户可一键反馈。

**S2-10 错误处理风格不统一**
散落的 `try/catch + toast(...)`，错误文案由各调用方自己写，
同一个"网络失败"在不同模块可能显示不同措辞。没有统一错误模型（code/message/可重试/上报与否）。

**S2-11 无重试与退避**
`sync.js` 单次超时即失败（技术债 #11）。商业化产品对网络操作标配指数退避 + 手动重试入口。

### 2.5 可访问性（差距最大的一块）

实测数据触目惊心：

| 检查项 | 实测数量 | 说明 |
|---|---|---|
| `aria-*` 属性 | **1** | 仅 `index.html` 背景层的 `aria-hidden` |
| `role` 属性 | **0** | 无 dialog/navigation/status 等角色 |
| `:focus-visible` | **0** | 键盘用户看不到焦点在哪 |
| `:focus` 声明 | 3 | 极少 |
| `tabindex` | 0 | 依赖 DOM 顺序（勉强可用） |
| `aria-live` | 0 | 读屏听不到 toast / 加载完成 |
| 键盘事件处理 | 1 | 仅 `user-idea` 的 keydown（`app.js:1785`） |

**S2-12 键盘完全不可用**
- 弹窗（`askDialog`）无法用 Esc 关闭
- 弹窗无**焦点陷阱**，Tab 会跑到弹窗背后的页面
- 关闭后**焦点不还原**到触发元素（键盘用户"丢失位置"）
- 无快捷键（商业化产品通常有 `Ctrl+K` 命令面板、`Ctrl+Enter` 提交、`Esc` 返回）

**S2-13 读屏用户几乎无法使用**
- Toast 无 `aria-live` → 操作结果不播报
- 视图切换无 `aria-current` / 焦点移动 → 不知道切到了哪
- 图标按钮（模式切换 `mode-toggle`、主题色 `swatch`、字体选项）**无 `aria-label`** → 读屏只念"按钮"
- 加载状态无 `aria-busy`

**S2-14 表单可访问性**
19 个 `<label>` 但 **0 个 `for=`**（靠包裹隐式关联，勉强可工作），
10 个 `placeholder` 中有多处**没有对应可见 label**（如搜索框、同步空间名），
placeholder 在输入后消失，对认知障碍用户不友好。

### 2.6 与商业化产品的差距（维度二小结）

| 能力 | 当前 | 商业化典型 |
|---|---|---|
| 视觉一致性 | 有令牌，但弹窗两套、内联样式泛滥 | 全自绘组件库，0 内联样式 |
| 操作反馈 | 单一 toast、1 处空状态 | 多类型通知、全局空状态体系、骨架屏 |
| 动效 | 有装饰动效，无 reduced-motion | 语义化动效 + 无障碍适配 |
| 异常处理 | 0 全局兜底 | 错误边界 + 上报 + 降级 UI |
| 键盘 | 基本不可用 | 全键盘可达 + 快捷键 |
| 读屏 | 基本不可用 | WCAG 2.1 AA 达标 |

**判断**：功能上"能用"，体验上距离"能卖"大约差一个量级。
但好消息是——**这些问题大多不依赖模块化**，可以立刻低成本修（见改进清单 P0 组）。

---

## 维度三：性能与稳定性

### 3.1 已做对的（不必重复投入）
- 流式渲染 O(n²) → O(n)（P1-8，`scheduleWrite` + rAF 合并）
- 本地推理每 token 重解码 → 增量解码（P1-9）
- `escapeHTML` 改为纯字符串替换（去掉 DOM 分配）
- `loadSettings()` 内存缓存（24 个调用点受益）
- 历史 50 条 / 墓碑 300 条上限（内存有界）
- `offline-llm.js` 对模型加载做 in-flight 去重、缓存索引

### 3.2 短板

**S3-1 零防抖/节流（实测 0 处）**
输入类交互（如优化页的 `oninput` 实时诊断）每次按键都跑全量分析。
商业化产品对文本输入默认 200–300ms 防抖。

**S3-2 零懒加载（实测 0 处，除 CDN 动态 import）**
17 个脚本 + 全部资源首屏一次性加载。
`frameworks.js`（432 行，222 条中英文案）、帮助页等**首屏不需要**的内容也一起下载。
商业化产品按路由/按需分割。

**S3-3 无生产可观测性**
无错误上报、无性能指标（FP/FID/LCP）、无用户行为埋点。
出问题只能靠用户描述。这是**从"个人项目"到"商业产品"最本质的差距之一**。

**S3-4 无请求取消的全局治理**
P0-3 修了在线模式的 signal 透传，但其它异步（云同步、内置服务拉取）未见统一取消机制。

### 3.3 模块化迁移中如何解决
- 防抖/节流收进 `core/` 工具层，所有输入类组件统一使用
- 模块化后天然可按 feature 做代码分割（`import()` 动态加载对比/设置/帮助）
- 错误上报做成 `core/monitor.js` 单点接入，全局 error/unhandledrejection 汇总
- 稳定性投入应**与模块化同步**：先加全局兜底（不依赖模块），再谈监控平台

---

## 维度四：配置化、可定制与国际化

### 4.1 已具备
- 个性化：主题色 6 色 + 自定义取色器、字体 3 种、背景氛围 5 种、明暗模式（`personalize.js`，实时预览 + 自动保存）
- API 服务商预设 6 个（`frameworks.js:423`）
- 内置在线服务 `service.json`（免配置新装用户）
- 6 种提示词版式 + 自动挑选（`style-variants.js`）
- 追问灵敏度三档、优化语言三档

### 4.2 短板

**S4-1 无国际化层，794 处硬编码中文**
实测分布（含中文的字符串字面量）：

| 文件 | 数量 |
|---|---|
| `app.js` | 243 |
| `frameworks.js` | 222 |
| `style-variants.js` | 56 |
| `optimizer.js` | 54 |
| `optimize-ui.js` | 51 |
| `local-model.js` | 45 |
| `pwa.js` | 35 |
| 其余 10 个文件 | 88 |
| **合计** | **794** |

注意：`frameworks.js` 的 222 处里，很大一部分是**提示词模板内容本身**——
这部分是否该国际化需要区分对待（提示词内容跟随用户语言更合理，UI 文案才需要 i18n）。
商业化产品会分层：**UI 文案（必译） / 提示词内容（可选，且通常保留原文效果更好）**。

**S4-2 无语言切换入口**
`opt-lang` 只控制"输出语言"（生成提示词用中文还是英文），**不控制界面语言**。

**S4-3 可定制维度偏"外观"**
有颜色/字体/背景，但缺：字号/密度、快捷键、默认框架、默认版式、是否自动保存历史、是否开机自启（桌面端）等。

**S4-4 配置无 schema 与迁移**
`pf_settings` 是自由对象，无版本字段、无迁移逻辑、无校验。
商业化产品会有 `settingsVersion` + 迁移函数，改字段名不会让老用户炸掉。

### 4.3 模块化迁移中如何解决
- i18n：**必须等模块边界清晰后再做**，否则 794 处字符串会在迁移中反复搬移。
  建议顺序：先拆模块 → 收敛文案到各模块的 `strings.js` → 接 i18n 层。
- 配置：抽 `core/settings/schema.js` + 迁移链，模块化时一并做（成本低，收益长期）
- 可定制：新增维度时统一走 schema，避免继续散落

---

## 维度五：测试、文档与可维护性

### 5.1 已有（这是本项目最强的部分之一）
- **454 项断言**，9 个套件 + `check-dist` + `verify-update-chain` + 启动器真机自测（22 项）
- 失败行顶格 `FAIL`，退出码与正则双重判定
- 关键套件含"校验器自检"，避免假绿
- 发布链路 `npm run release` 五段防呆

### 5.2 短板

**S5-1 测试环境是自制沙盒，不是真实浏览器**
`.workbuddy/_check-lib.mjs` 用 `node:vm` + 极简 DOM。
它能验证**逻辑与契约**，但**无法验证**：
- 真实布局/样式/响应式
- 可访问性（读屏、焦点环、对比度）
- 真实渲染性能
- 跨浏览器兼容

商业化产品会用 Playwright/Cypress 做真实浏览器 E2E + axe 做可访问性扫描。
**这正是维度二问题长期没被发现的根本原因**——没有工具能暴露它们。

**S5-2 无标准测试框架与 lint（实测全 0）**
`.eslintrc` / `.prettierrc` / `tsconfig.json` / `vitest.config.js` / `playwright.config.js` **均不存在**。
`devDependencies` 只有 `@tauri-apps/cli`。
后果：无静态检查（拼写、未使用变量、潜在 undefined 全靠人眼）、无统一格式（多人协作必冲突）。

> ⚠️ 环境提示：当前终端 `npm` 不可用（`WinError 2`），引入上述工具链需在可用的终端执行。

**S5-3 文档结构失衡**
8 篇 `.md`：`README` + `LOCAL_MODEL_README` + `PWA_INSTALL_GUIDE` + `MULTIMODAL_AND_STYLES`
+ `CODE_REVIEW` / `CODE_REVIEW_2026-09-13` / `PROJECT_AUDIT_REPORT` / `TEMPLATE_TEST_REPORT`。
即：**一半是审查/测试报告（面向过去），缺面向未来的文档**：
- 无架构文档（模块边界、数据流、扩展点）
- 无贡献指南（如何新增一个框架/模板/版式）
- 无 CHANGELOG（用户不知道版本间变化，只有发布说明）
- 无用户手册（虽有 README，但设置项/离线模型/更新的操作路径未成体系）

**S5-4 无 JSDoc / 类型**
函数无统一注释规范，跨模块调用靠读源码。

### 5.3 模块化迁移中如何解决
- 迁移同时落地 ESLint + Prettier（先 `no-undef` 一类安全规则，避免一开始满屏报错）
- 沙盒测试继续保留（它跑得快、对逻辑契约有效），**另建一层真实浏览器 E2E**覆盖关键路径
- 补架构文档与贡献指南——**这不只是文档，更是模块边界的设计稿**：写不出来就说明边界没想清楚

---

## 改进清单（按投入产出比排序）

> 工作量按"1 名熟悉本项目的开发者"估算，单位：人天。
> P0 = 立即做（不依赖模块化，收益立竿见影）
> P1 = 与模块化并行
> P2 = 模块化完成后

### P0：不依赖模块化，立刻可做（合计约 9–13 人天）

| # | 改进项 | 解决什么 | 前置依赖 | 工作量 | 收益 |
|---|---|---|---|---|---|
| 1 | **统一弹窗**：替换 4 处原生 `confirm()`，给 `askDialog` 补 `role="dialog"`/`aria-modal`/焦点陷阱/Esc 关闭/焦点还原 | S2-1 S2-12 | 无 | **1.5** | 消除最刺眼的不一致；键盘可用 |
| 2 | **全局错误兜底**：`window.onerror` + `unhandledrejection` + 友好提示 UI | S2-9 S3-3 | 无 | **1.5** | 从"白屏无感知"到"可提示可反馈" |
| 3 | **Toast 增强**：4 种类型（成功/错误/警告/信息）+ 队列 + `aria-live` + 按时长自适应 | S2-3 S2-4 S2-13 | 无 | **1** | 操作结果可读、读屏可播报 |
| 4 | **焦点可见性**：`:focus-visible` 全局样式（约 20 行 CSS） | S2-12 | 无 | **0.5** | 键盘用户立刻能用 |
| 5 | **`prefers-reduced-motion`** 适配 | S2-7 | 无 | **0.5** | WCAG 合规 + 健康风险 |
| 6 | **输入框防抖**：文本类实时分析统一 250ms | S3-1 | 无 | **0.5** | 长文本输入不再卡 |
| 7 | **空状态补齐**：模板筛选无结果、对比未输入、模型未下载等 | S2-5 | 无 | **2** | 消除"点了没反应"的困惑 |
| 8 | **骨架屏/加载态**：历史、模板、设置三个视图 | S2-6 | 无 | **1.5** | 消除切换闪白 |
| 9 | **ARIA 语义补齐**：图标按钮 `aria-label`、`aria-current`、视图切换焦点移动 | S2-13 | 建议先做 #4 | **2** | 读屏可用性从 0 到基本可用 |

### P1：与模块化并行（合计约 12–18 人天）

| # | 改进项 | 解决什么 | 前置依赖 | 工作量 | 收益 |
|---|---|---|---|---|---|
| 10 | **引入 ESLint + Prettier**（先开安全规则集） | S5-2 | 需要可用 npm | **1** | 静态拦下低级错误 |
| 11 | **app.js 拆分（核心）**：按 feature 切分，装配层只做组合 | S1-1 S1-3 | 先定目录结构 + 出口 | **6** | 可维护性的根本转折 |
| 12 | **状态管理极简 store**：替换裸 `state` 全局对象，带订阅 | S1-4 | #11 进行中 | **2** | 消除字段蔓延 |
| 13 | **SW 清单自动生成**：从打包产物生成 `STATIC_CACHE_URLS`，删除手工 4 处同步 | S1-2 | #11 | **1.5** | 消灭一类高频事故 |
| 14 | **真实浏览器 E2E**（Playwright）+ axe 可访问性扫描 | S5-1 维度二 | 需要可用 npm | **3** | 让 UI 问题"能被发现" |
| 15 | **统一错误模型 + 上报**：错误码/可重试/是否上报，接 Sentry 类或自建 | S2-10 S3-3 | #2 | **2** | 生产可观测 |
| 16 | **配置 schema + 版本迁移** | S4-4 | #12 | **1.5** | 改设置字段不再炸老用户 |

### P2：模块化完成后（合计约 12–20 人天）

| # | 改进项 | 解决什么 | 前置依赖 | 工作量 | 收益 |
|---|---|---|---|---|---|
| 17 | **代码分割 / 按需加载**：对比、设置、帮助、框架库按路由懒加载 | S3-2 | #11 | **3** | 首屏体积显著下降 |
| 18 | **国际化分层**：UI 文案抽 `strings.js` → i18n 层（794 处，但许多是提示词内容，需先分层） | S4-1 S4-2 | #11（必须） | **6** | 打开非中文市场 |
| 19 | **快捷键体系**：`Esc` 返回、`Ctrl+Enter` 提交、可选命令面板 | S2-12 | #4 #1 | **3** | 效率用户留存 |
| 20 | **内联样式收敛到设计令牌** | S2-2 | #11 | **3** | 主题一致性、可换肤 |
| 21 | **架构文档 + 贡献指南 + CHANGELOG** | S5-3 | #11 | **2** | 协作与长期维护 |
| 22 | **可定制维度扩展**：字号/密度、默认框架与版式、自动保存开关 | S4-3 | #16 | **3** | 满足不同使用习惯 |

---

## 建议的落地顺序（关键路径）

```
第 1 周（P0，约 9–13 人天）
  #4 焦点可见 → #5 reduced-motion → #6 防抖        （半天内全部完成，风险极低）
  #1 统一弹窗 → #2 全局错误兜底 → #3 Toast 增强     （交互与稳定性地基）
  #7 空状态 → #8 骨架屏 → #9 ARIA                  （体验补齐）
  ↓ 这周结束后：产品从"能用"变成"用起来不别扭"

第 2–4 周（P1 启动模块化）
  #10 ESLint 就位 → #11 目录结构 + app.js 拆分（迭代式，每拆一个 feature 跑一次全量校验）
  #12 store → #13 SW 自动生成
  #14 E2E + axe（让后续 UI 改动有网）
  ↓ 这阶段结束：新增功能不再需要动 app.js

第 5 周起（P2）
  #17 代码分割 → #18 i18n → #19 快捷键 → #20 样式收敛 → #21 文档 → #22 可定制
```

**三条硬约束（务必遵守）**

1. **模块化必须与校验同步推进**：现有 454 项断言是唯一的回归网。
   每拆一个模块立即跑 `run-all-checks`，并**为拆出来的模块补一个套件**。
   历史上这个项目的灾难（9 个脚本丢失、`callLLM` 递归覆盖）都源于"改了但没校验"。
2. **i18n 不要提前做**：794 处字符串在模块边界未定时提取 = 白做一遍。
   必须等 #11 完成。
3. **不要一次性切 `<script type="module">`**：17 个文件的加载顺序是契约，
   建议先"目录模块化 + 保持经典脚本加载"，稳定后再切 ESM。

---

## 附：本次分析用到的量化依据

| 指标 | 数值 | 来源 |
|---|---|---|
| JS 文件数 / 总行数 | 17 / 约 3600 行 | `js/` 扫描 |
| `app.js` 规模 | 1814 行 / 70KB | 文件统计 |
| CSS 变量 / keyframes / transition / media | 38 / 6 / 24 / 10 | `css/style.css` |
| `prefers-reduced-motion` / `:focus-visible` | 0 / 0 | `css/style.css` |
| `aria-*` / `role` / `tabindex` | 1 / 0 / 0 | `index.html` |
| 语义 `<button>` / `div onclick` | 95 / 0 | `index.html` |
| 原生 `confirm()` | 4 处 | app.js:945、export.js:78、local-model.js:486、offline-ui.js:182 |
| 空状态 | 1 处 | app.js:907 |
| 全局错误兜底 | 0 | 全 `js/` 扫描 |
| 防抖/节流 / 懒加载 | 0 / 0 | 全 `js/` 扫描 |
| 含中文字符串字面量 | 794 | 全 `js/` 扫描 |
| 自动化断言 | 454 | `run-all-checks` |
| lint/format/类型/E2E 配置 | 全 0 | 根目录扫描 |
| `.md` 文档 | 8（4 篇为审查/测试报告） | 根目录扫描 |
