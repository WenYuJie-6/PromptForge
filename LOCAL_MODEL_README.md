# PromptForge 智能引擎系统使用说明

## 🚀 功能概述

PromptForge 现已支持智能双引擎系统，用户可以在线 API 和本地离线模型之间无缝切换，享受最佳的使用体验。

### 🎯 双引擎架构

| 引擎类型 | 特点 | 适用场景 | 优势 |
|---------|------|----------|------|
| **在线 API** | 响应快速，功能强大 | 需要高质量输出，网络良好 | 最新模型，速度快 |
| **本地离线** | 保护隐私，无需联网 | 隐私敏感，网络受限 | 完全离线，数据安全 |

## 🎨 专业离线模型管理界面

### 核心特性
- ✅ **专业卡片设计**: 清晰的模型信息展示
- ✅ **智能状态管理**: 实时显示模型加载状态
- ✅ **一键操作**: 下载加载、卸载、清除缓存
- ✅ **设备信息检测**: 自动检测设备性能和后端支持
- ✅ **实时进度显示**: 美观的进度条和状态提示

## 📋 使用流程

### 1. 选择运行引擎

#### 在线 API 模式
1. 打开设置页面
2. 选择"运行引擎" → "在线 API"
3. 配置 API 端点、密钥和模型
4. 点击"保存设置"

#### 本地离线模式
1. 打开设置页面
2. 选择"运行引擎" → "本地离线"
3. 选择模型（专业卡片界面）
4. 点击"下载加载"按钮
5. 查看实时进度
6. 模型加载完成后即可使用

### 2. 离线模型管理

#### 模型卡片界面
```
┌─────────────────────────────────────────────────────────────┐
│ 🌟 Qwen2.5-1.5B [推荐]                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 已加载                                                  │ │
│ │                                                         │ │
│ │ 中文能力优秀，提示词优化效果最好                          │ │
│ │ 大小：~1.5GB                                            │ │
│ │                                                         │ │
│ │ [卸载] [清除缓存]                                       │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 操作说明
- **下载加载**: 点击下载并加载选中的模型
- **卸载**: 卸载已加载的模型，释放内存
- **清除缓存**: 删除模型缓存，需要重新下载

## 🔧 技术特性

### 智能切换
- 一键切换在线/离线模式
- 自动保存用户偏好设置
- 无缝切换，不中断工作流

### 本地模型优势
- **隐私保护**: 数据完全在本地处理
- **离线使用**: 无需网络连接
- **快速响应**: 本地推理，延迟更低
- **成本控制**: 无需 API 调用费用

### 设备检测
- **WebGPU**: 硬件加速，性能最佳
- **WASM**: 纯 CPU 运行，兼容性好
- **内存检测**: 自动检测设备内存
- **CPU 信息**: 显示处理器核心数

## 📊 性能对比

| 维度 | 在线 API | 本地离线 |
|------|----------|----------|
| 响应速度 | 极快（100-500ms） | 中等（1-3秒） |
| 模型质量 | 最新最优 | 良好（1.5B-2.2B） |
| 隐私保护 | 依赖服务商 | 完全本地 |
| 网络依赖 | 需要 | 不需要 |
| 成本 | 按使用付费 | 一次性下载 |
| 设备要求 | 任何设备 | 中高端设备 |

## 🎨 界面指南

### 离线模型管理界面

#### 1. 引擎选择
```
生成引擎:
├── 在线（需要 API）    [默认]
└── 离线（本地模型）   [推荐]
```

#### 2. 模型卡片
```
🌟 Qwen2.5-1.5B [推荐] ──┐
    中文能力优秀，提示词优化效果最好    │
    大小: ~1.5GB                    │
    [已加载] ── 卸载                │
    [清除缓存]                      │
                                   │
🚀 Phi-3.5 Mini ──────────────┤
    英文能力强，通用推理能力好        │
    大小: ~2.2GB                    │
    [下载加载] ── 点击下载          │
    [清除缓存]                      │
                                   │
📱 SmolLM2-1.7B ──────────────┘
    最轻量，适合手机等低内存设备      │
    大小: ~1.0GB                    │
    [下载加载] ── 点击下载          │
    [清除缓存]                      │
```

#### 3. 进度显示
```
正在下载 Qwen2.5-1.5B... 45%
[████████████████░░░░░░░░░░░░░░] 45%
```

#### 4. 设备信息
```
设备信息：
WebGPU（硬件加速） · 8GB RAM · 8 核 CPU
```

## 🛠️ 详细配置

### 在线 API 配置
```javascript
{
  engine: 'online',
  endpoint: 'https://api.deepseek.com/v1',
  key: 'sk-your-api-key',
  model: 'deepseek-chat',
  lang: 'zh',
  sensitivity: 'medium'
}
```

### 本地离线配置
```javascript
{
  engine: 'offline',
  localModel: 'qwen25', // qwen25, phi35, smollm
  lang: 'zh',
  sensitivity: 'medium'
}
```

## 🚨 注意事项

### 在线 API 模式
- 需要稳定的网络连接
- API 密钥仅保存在当前会话
- 注意 API 调用成本
- 不同服务商模型能力差异

### 本地离线模式
- 首次下载需要较长时间
- 模型文件占用存储空间（1.0-2.2GB）
- 需要较新的浏览器支持
- 推理速度取决于设备性能
- WebGPU 需要较新的 GPU 和驱动程序

## 🔍 故障排除

### 引擎切换问题
- **问题**: 切换引擎后无响应
- **解决**: 刷新页面重新加载
- **检查**: 确保浏览器支持相关功能

### 本地模型问题
- **下载失败**: 检查网络连接，尝试重新下载
- **加载缓慢**: 关闭其他浏览器标签，释放内存
- **推理卡顿**: 选择更小的模型或升级设备
- **WebGPU 不支持**: 自动降级到 WASM 模式

### 在线 API 问题
- **连接失败**: 检查端点地址和密钥
- **模型不支持**: 更换支持的模型
- **超时错误**: 检查网络稳定性

## 📁 文件结构

```
PromptForge/
├── js/unified-llm.js      # 统一调用层
├── js/offline-llm.js       # Transformers.js 核心
├── js/offline-ui.js        # 离线 UI 控制（新增）
├── js/local-model.js       # 本地模型管理（已更新）
├── js/api.js               # API 调用（已更新）
├── js/app.js               # 主应用逻辑（已更新）
├── index.html              # 主页面（已更新）
├── css/style.css           # 样式（已更新）
└── LOCAL_MODEL_README.md   # 使用说明
```

## 🎯 最佳实践

### 1. 根据场景选择
- **工作环境**: 优先使用在线 API，获得最佳性能
- **个人隐私**: 使用本地离线模式，保护数据安全
- **网络受限**: 本地离线模式保证可用性

### 2. 设备适配
- **高端设备**: 可使用本地大模型获得更好效果
- **移动设备**: 推荐使用 SmolLM2 节省电量
- **开发环境**: 在线 API 获得最新模型能力

### 3. 成本优化
- **测试阶段**: 使用本地模型节省 API 费用
- **生产环境**: 根据需求选择在线 API
- **长期使用**: 混合使用两种模式

### 4. 内存管理
- **大模型**: 8GB+ 内存，推荐 Qwen2.5-1.5B
- **小模型**: 4GB+ 内存，推荐 SmolLM2-1.7B
- **移动设备**: 2GB+ 内存，推荐 SmolLM2-1.7B

## 🔮 未来计划

- [ ] 支持更多本地模型
- [ ] 优化本地推理性能
- [ ] 添加模型性能测试
- [ ] 支持自定义模型导入
- [ ] 引擎自动推荐系统
- [ ] 模型热切换功能

## 🤝 贡献

如果您在使用过程中发现问题或有改进建议，欢迎提交 Issue 或 Pull Request。

---

**提示**: 新的离线模型管理界面让模型选择和管理变得更加直观和易用！🎉