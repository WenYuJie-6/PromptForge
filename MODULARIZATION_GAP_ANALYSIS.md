# PromptForge 模块化迁移前：与成熟商业化软件的差距分析

> **产出方式**：软件开发团队 SOP「仅需分析」工作流。
> 主理人 齐活林（交付总监）编排；产品经理 许清楚（维度 2）、架构师 高见远（维度 1/3/4 + 迁移方案）、QA 严过关（维度 5 + 质量门禁）分别独立分析，由主理人汇总对齐。
>
> **事实底座**：基于 `main` 分支实际代码扫描（文件 + 行号），非主观印象。
> 分析时的代码规模：`js/` 17 个脚本约 3600 行，`app.js` 1814 行 / 70KB，`css/style.css` 575 行，`index.html` 649 行，`src-tauri/src/lib.rs` 约 890 行。已有 **454 项自动化断言**、发布链路五段防呆、CI 已跑通。

---

# 一、执行摘要

## 1.1 总体判断

> **PromptForge 是一个「功能完备度高、工程化地基薄」的成熟单页应用。**
> 它把「三形态分发 + 更新链路 + 离线推理」这类难活做扎实了（交付链路定级 ⭐4/5、热路径性能 ⭐4/5），
> 却因为没有模块系统，把复杂度全压在两条**隐形契约**上——**加载顺序**与**全局词法绑定**。
>
> 最大的差距不在功能，而在两处：
> ① **可访问性与交互一致性的系统性缺失**（全站 1 个 `aria-*`、0 个 `:focus-visible`、0 处动效降级）；
> ② **无模块边界导致任何改动都要靠人脑推演全局依赖**。
>
> 第三个容易被忽略的结论：**454 项断言全绿与维度二满目疮痍并不矛盾——那是观测盲区，不是质量证明。**

## 1.2 统一评级表

⭐1 = 差距最大，⭐5 = 已达标。

| 维度 | 定级 | 一句话诊断 |
|---|---|---|
| **1 架构与模块化** | ⭐⭐ | 无模块边界、无依赖推导、无类型安全；删一个文件要改 4 处 |
| **2 界面交互与体验** | ⭐⭐ | 基础设施级缺失：错误兜底/模态语义/通知/降级/无障碍五条线全为 0 |
| **3 性能与稳定性** | ⭐⭐⭐⭐ | 热路径已优化到位；缺防抖、代码分割、生产可观测性 |
| **4 配置化与国际化** | ⭐⭐⭐ | 794 处硬编码；配置无 schema/无迁移；可定制偏"外观" |
| **5 测试与文档** | ⭐⭐⭐ | 护栏意识极强，但观测面只覆盖"逻辑与字符串契约" |

**分项细分评级**（架构师提供）：模块化 1 / 依赖管理 1 / 类型安全 1 / 可扩展性 2 / 可测试性 3 / 交付链路 4 / 性能热路径 4。

## 1.3 三条最重要的结论

1. **不要跳过护栏直奔打包器。** 本项目的测试与缓存链路是**硬编码**的
   （`check-ui-wiring.mjs` 硬断言"17 个脚本 + 固定顺序"；`_check-lib.mjs` 用 `node:vm` 复刻经典脚本绑定；
   `sync-dist`/`check-dist` 对 `js/` 做非递归计数）。一次性切 `<script type="module">`
   会**同时击穿测试、缓存、打包三条链路**，且不可回滚。

2. **UI/无障碍的大头与"是否模块化"弱相关，可以立刻做。** 三位成员独立得出同一结论：
   动效降级、焦点可见、Dialog 语义、Toast 播报、错误兜底、`label` 绑定——**全是纯加法、零回归风险**，
   不需要打包器、不需要模块边界，也不需要 `npm`。

3. **i18n 必须等 `app.js` 拆分之后**，且必须**二分处理**：
   UI 文案（约 500 处）→ i18n 词条；模型指令与示例语料（约 150 处，如 `buildPrompt` 正文、`TEMPLATES.idea/tip`）
   → **版本化 prompt pack 数据资产，不翻译**。把模型指令做成语言包会导致不同语区模型行为漂移。

---

# 二、维度 1：架构与模块化程度

## 2.1 现状实测

| 项 | 实测 |
|---|---|
| 模块机制 | **无**。`index.html` 17 个 `<script src>` 顺序加载，跨文件靠顶层 `const` 全局词法绑定 |
| `app.js` | **1814 行 / 70KB**，顶层 79 个函数，承担 **9 类职责**：状态机、评分、对比、分享、历史、模板、设置、更新检查、引擎编排 |
| 跨文件引用 | 26 处显式 `window.X =` 补挂；其余依赖词法绑定 |
| DOM 契约 | 60+ 处 `getElementById`，以字符串 id 为契约 |
| 全局状态 | `const state = {...}`（`app.js:8`）被 6+ 文件直接改写；`state._autoReason` 未在字面量声明（`app.js:631` 才首次赋值）——隐式字段 |
| 删除一个 `js/` 文件 | 需同步 **4 处**：文件本身、`index.html` 的 `<script>`、`sw.js` 的 `STATIC_CACHE_URLS`、`sw.js` 的 `CACHE_NAME` 版本号 |

## 2.2 四类耦合

1. **顺序耦合** —— 依赖靠 `<script>` 次序传递，改顺序即静默崩溃
2. **全局状态耦合** —— 裸全局对象被多处直接改写，无订阅/派发
3. **UI 与逻辑混写** —— `app.js` 同文件内既做状态机又做 9 类视图逻辑
4. **字符串 id 契约** —— HTML/JS 解耦仅由 `check-ui-wiring` 的正则兜底

## 2.3 与商业化产品差距

| 维度 | 商业化基线 | 现状 | 差距 |
|---|---|---|---|
| 模块边界 | 显式 import/export，可环检测 | 全局词法绑定 | ⭐⭐ |
| 依赖声明 | 静态可解析 | 顺序 + `window` 补挂 | ⭐⭐ |
| 类型安全 | TS / JSDoc + CI 校验 | 0 | ⭐⭐ |
| 错误边界 | 组件级 + 全局兜底 | 0 | ⭐⭐ |
| 模块级单测 | 可 import 即可测 | 仅静态/行为断言 | ⭐ |

## 2.4 迁移中如何解决
见第七章「模块化迁移方案」。核心原则：**先冻边界再搬内部**；每模块只经一个 `PF.<域>` 出口暴露；
迁移期加兼容垫片让旧全局名继续存在，保证行为不变。

---

# 三、维度 2：界面交互与用户体验（重点）

## 3.1 总体定级：⭐⭐（差距最大）

功能可用性不错（个性化 6 色/3 字体/5 氛围、明暗模式、实时预览全部落地），但**交互质量停留在"能用"层**。
问题不是某个组件没做好，而是**基础设施级缺失**：错误兜底、模态框语义、通知、动效降级、可访问性——五条线全部为 0。

## 3.2 视觉一致性

**短板**
- 38 个设计令牌本可成为统一真相源，却因**内联样式绕过**而形同虚设：设置页约 30 处 `style="..."`
- `askDialog`（`app.js:1147/1148`）自己也在模板串里写内联样式
- **弹窗两套风格并存**：自建 `askDialog`（`app.js:1139`）+ **4 处原生 `confirm()`**（`app.js:945`、`export.js:78`、`local-model.js:486`、`offline-ui.js:182`）
  → 原生弹窗无法主题化、无法本地化、无法危险色、Tauri 下尤其突兀

**商业化做法**：Raycast / Obsidian 用**单一令牌层 + 单一 Dialog primitive**，所有弹窗（确认/危险/表单）复用；
内联样式被 lint 规则禁止。

**迁移中解决**：抽 `tokens.css` + 统一 Dialog **不依赖模块化，可立刻做**；
"禁止内联样式"的 lint 门禁需等构建链出现后再加。

## 3.3 操作反馈

**短板**
- `toast()`（`app.js:1086`）实现就是 `textContent + classList.add('show') + setTimeout(2500)`
  → 单一元素、**无类型区分**（成功/错误/警告同貌）、**无队列**（后到覆盖先到）、**无 `aria-live`**、无手动关闭
- 空状态**全站仅 1 处**（`app.js:907` `history-empty`）——模板筛选无结果、对比未输入、模型未下载等均裸白屏
- 加载态仅框架对比一处骨架屏（`app.js:289` `renderCompareSkeleton`），其余异步靠按钮 disabled 硬等
- **无乐观更新/撤销**——删除历史、清空数据都是"先拦截式确认再不可逆执行"

**商业化做法**：Notion / VS Code 用**分级 Toast + 可撤销 Ops（Undo 条 5–8s）**，
破坏性操作"先执行 + 给撤销窗口"而非拦截式确认；每个可为空的容器都有设计过的空状态插画 + CTA。

**迁移中解决**：Toast 升级 + 空状态补齐**可立刻做**；乐观更新依赖状态层收敛，**等 store 边界清晰后再做**。

## 3.4 动效

**短板**
- 6 个 `@keyframes` + 24 处 `transition`，但**全是装饰性**（背景光斑、hover）
- 缺语义动效：无进入/退出过渡、无列表增删 FLIP、无视图切换方向感
- **`prefers-reduced-motion` 覆盖为 0** —— 前庭功能障碍用户无法关闭背景光斑与过渡，**无障碍硬伤**

**商业化做法**：VS Code / Linear 所有动效走统一 motion token，并在
`@media (prefers-reduced-motion: reduce)` 下一键降为 opacity 快切。

**迁移中解决**：**纯 CSS，完全不依赖模块化，半天可完成**——性价比最高的一次性改动。

## 3.5 异常与空状态处理

**短板**
- **全局错误兜底为 0**：无 `window.onerror`、无 `unhandledrejection`、无错误边界
  → 任一异步链路（LLM 调用、离线模型加载）抛未捕获异常，用户只见"点了没反应"或白屏，**没有任何可诊断信息**
- 错误处理风格极不统一：`askDialog` 走 Promise，4 处 `confirm()` 同步阻塞，其余散落 `try/catch + toast`
- **无重试退避**——网络/模型失败只能手动重试（技术债 #11 `sync.js`）

**商业化做法**：Raycast 有全局错误边界 + 崩溃上报 + "重试"一键恢复；网络层统一 retry/backoff。

**迁移中解决**：全局兜底 + 统一提示**可立刻做**；统一 retry 策略与错误边界**等模块化后的服务层**。

## 3.6 可访问性（最严重）

| 检查项 | 实测 | 后果 |
|---|---|---|
| `aria-*`（全站） | **1**（仅背景层 `aria-hidden`） | 读屏无法理解结构 |
| `role=` | **0** | 无 dialog / nav / status 语义 |
| `:focus-visible` | **0** | 键盘用户看不到焦点在哪 |
| `<label for=>` | **0**（19 个 `<label>` 无关联，10 个 `placeholder` 无可见 label） | 表单可访问性不合格 |
| `tabindex` | 0 | 依赖 DOM 顺序 |
| 键盘事件（全 `js/`） | **1**（仅 `app.js:1785`） | 几乎无键盘支持 |

**具体后果**
- `askDialog`（`app.js:1139`）**无 `role="dialog"`/`aria-modal`、无焦点陷阱、无 Esc 关闭、关闭后不还焦**
  → 键盘/读屏用户**进不去也出不来**，而它是全站破坏性操作的必经关卡 —— 这是**功能性阻断**，不是体验瑕疵
- 导航全是 `<button onclick>`，无 `aria-current`
- Toast 无 `aria-live`，读屏完全不播报操作结果

**商业化做法**：Obsidian / Notion 组件均带 focus trap + restore、`aria-current`、live region、全键盘可达。

**迁移中解决**：Dialog 焦点管理 + Esc + 还焦、Toast `aria-live`、`label for` 绑定、导航 `aria-current`
—— **全部不依赖模块化，可立刻做**，是本维度 ROI 最高的一批；
系统性 a11y 审计（对比度、landmark、skip-link）等组件化后统一治理。

## 3.7 与商业化产品对照总表

| 能力 | 当前 PromptForge | 商业化典型 |
|---|---|---|
| 模态框 | 自建 `askDialog` 无 role/Esc/焦点；另 4 处原生 `confirm` | 统一 Dialog primitive，焦点陷阱 + 还焦 + Esc |
| 通知 | 单一 Toast，固定 2.5s，无类型/队列/aria-live | 分级 Toast + 队列 + live region + 可操作按钮 |
| 空状态 | 仅 1 处 | 每容器设计过空状态 + CTA |
| 加载态 | 1 处骨架屏 | 骨架屏 / 进度 / 流式占位齐全 |
| 撤销 | 无（靠拦截式确认） | 乐观更新 + Undo 窗口 |
| 错误兜底 | 0 | 全局边界 + 上报 + 重试 |
| 动效降级 | `prefers-reduced-motion` 0 覆盖 | 一键降级为快切 |
| 键盘可达 | 唯一 1 处键盘处理 | 全键盘可达 + 快捷键体系 |
| 读屏 | aria 1 / role 0 | 语义化 landmark + live region |
| 表单 | `label for` 0 | 全 label 关联 + 错误提示 |

## 3.8 最容易被忽视但影响最大的 3 项（PM 判断）
1. **`prefers-reduced-motion` 为 0** —— 被视为"小细节"，实为无障碍合规红线，修复成本极低，性价比被严重低估
2. **`askDialog` 缺焦点陷阱与 Esc** —— 键盘用户实际"进得去出不来"，是**功能性阻断**
3. **全局错误兜底为 0** —— 任何未捕获异常都退化为"点了没反应"，用户无法自助、团队无法定位，**最隐蔽的信任杀手**

---

# 四、维度 3：性能与稳定性

## 4.1 已做对的（定级 ⭐4，不必重复投入）
rAF 合并流式写入（O(n²)→O(n)）、离线推理增量解码、`escapeHTML` 纯字符串替换、
`loadSettings()` 内存缓存、历史 50 条 / 墓碑 300 条上限、离线模型加载 in-flight 去重、
更新链路 6 层兜底、`dist-app` 与 `dist` 职责分离。
**热路径质量高于同类个人项目。**

## 4.2 仍缺

| 问题 | 实测 | 后果 |
|---|---|---|
| **零防抖/节流** | 全文仅 12 处 `setTimeout`，无 debounce/throttle | 输入即时诊断、窗口 resize、更新轮询（45s）全在裸奔 |
| **零可观测性** | 无 `onerror`/`unhandledrejection`/错误边界 | 生产崩溃 = 静默白屏；同类故障只能靠用户口述复现，修复周期从天级起 |
| **请求治理只做一半** | 有 `activeControllers`，但无统一超时、无重试、无并发上限 | 悬挂请求 |
| **零代码分割** | 除 `offline-llm.js` 一次 CDN 动态 import 外全量首屏加载 | 首屏体积无优化空间 |

## 4.3 差距对照

| 维度 | 商业化基线 | 现状 | 差距 |
|---|---|---|---|
| 防抖/节流 | 输入/搜索/resize 全覆盖 | 0 | ⭐⭐ |
| 可观测性 | 错误上报 + 埋点 + 日志 | 0 | ⭐⭐ |
| 请求治理 | 超时 + 重试 + 取消 + 并发上限 | 仅取消 | ⭐ |
| 代码分割 | 路由/重资源懒加载 | 0 | ⭐⭐ |

## 4.4 一个尖锐的观察
本项目**历史上反复出现的故障**（"检查更新永远显示最新版本"、`callLLM` 无限递归、离线白屏），
共同特征都是**静默失败**。而**无生产可观测性正是"静默失败"能长期潜伏的原因**——
修复周期从分钟级拉到天级。所以"加日志/上报"不是锦上添花，是**止损**。

---

# 五、维度 4：配置化、可定制与国际化

## 5.1 794 处硬编码中文的处置——**必须二分，不能一刀切**

实测分布：`app.js` 243、`frameworks.js` 222、`style-variants.js` 56、`optimizer.js` 54、
`optimize-ui.js` 51、`local-model.js` 45、`pwa.js` 35，其余 10 个文件 88。

**判据**：字符串最终**进入 DOM 文本节点** → i18n 词条；**进入 LLM 请求体 / 作为生成数据** → **数据资产，不翻译**。

| 类别 | 数量（估） | 处置 |
|---|---|---|
| **UI 文案** —— `app.js` 的 toast/标签/帮助页、`framework.name/desc`、`TEMPLATES.name/desc/cat`、`API_PRESETS.hint`、pwa/personalize 界面文字 | 约 **500** | 抽 i18n 词条（`t(locale)`） |
| **模型指令与语料** —— `buildPrompt` 正文、`FRAMEWORK_DIMENSIONS`、`TEMPLATES.idea/tip` | 约 **150** | **版本化 prompt pack 数据**，不翻译 |

**为什么不翻译那 150 处**：框架本身已声明「输出语言跟随输入」，把模型指令做成语言包会导致
**不同语区模型行为漂移、质量不可控**。它们应当作为可选的**多语言资产包**存在（如 `data/prompt-packs/en-US/`），
而不是 i18n 词条。

## 5.2 配置演进风险
`pf_settings` 是**自由对象**，无 `schemaVersion`、无迁移链、无校验。
- 加字段：安全
- **改字段名/改语义：老用户配置静默错乱** —— 历史上 `set-local-mode` 等遗留控件就是这类遗产
  （代码里至今保留空值保护，正是这种遗留的痕迹）

## 5.3 可定制维度偏"外观"
已有：6 色 + 自定义取色器、3 字体、5 背景、明暗模式。
缺：**行为可定制** —— 默认框架、默认输出语言、默认版式、快捷键、导出模板、是否自动保存历史。

## 5.4 差距对照

| 维度 | 商业化基线 | 现状 | 差距 |
|---|---|---|---|
| i18n | 词条外置 + 多语 | 硬编码 794 处 | ⭐⭐ |
| 配置演进 | schema + 迁移 + 版本 | 裸对象 | ⭐⭐ |
| 可定制维度 | 行为 + 外观 | 偏外观 | ⭐ |

---

# 六、维度 5：测试、文档与可维护性

## 6.1 总体定级：⭐⭐⭐

护栏意识与执行力**远超同类个人项目**（454 断言 + 发布链五段防呆 + 校验器自检 + 负向测试），
但护栏的**观测面**只覆盖"逻辑与字符串契约"，对"渲染、可访问性、真实交互、性能"**零覆盖**。

## 6.2 能力矩阵（关键诊断）

| 能力 | 覆盖 | 说明 |
|---|---|---|
| 纯逻辑/数据完整性 | ✅ | 框架/模板/分类、`buildPrompt` 四种入参、模型元数据单一事实源、设置缓存三纪律 |
| 静态字符串契约 | ✅ | `<script>` 顺序、`sw.js` 清单一致性、内联 handler 有定义、无幽灵 id |
| 沙盒行为级验证 | ✅ | 下载流程 4 环境、17 脚本整站冒烟、XSS 注入面、样式幂等 |
| 回归护栏 | ✅ | 更新链路 + CI 顺序、构建前预检（mtime 判新鲜度）、客户端判定复刻、启动器真机自测 |
| **真实布局/渲染** | ❌ | 极简 DOM 丢弃文本节点、无布局引擎 → 内联样式绕过令牌、骨架屏缺失、切视图闪白**不可见** |
| **可访问性** | ❌ | 无无障碍树、无焦点管理、`focus()` 是空实现 → aria-1/role-0/focus-visible-0 **全部隐形** |
| **真实浏览器兼容** | ❌ | `node:vm` ≠ 浏览器；无 WebView2 差异、无真实 SW 生命周期 |
| **真实性能** | ❌ | 无 layout/paint、无真实计时 → 0 防抖、渲染复杂度回归测不到 |
| **视觉回归** | ❌ | 无像素/截图比对 |
| **键盘/真实交互** | ❌ | 无 tab 序、无 Esc/焦点陷阱 → 弹窗键盘不可用不可见 |

**根因**：`_check-lib.mjs` 的 DOM 是"为断言而造"的最小实现，
**只能断言作者能预先枚举的静态/行为事实**。凡需渲染引擎才能暴露的问题，在该层**结构上不可测**。

> 这就是"454 全绿 vs 维度二满目疮痍"的解释：**不是质量问题，是观测盲区**。

## 6.3 沙盒 vs 真实浏览器：分层组合

- **保留沙盒**：进程内、亚秒级、零依赖、确定无 flake；对"无打包器 + 经典脚本"尤其贴切
  —— 它是按 `index.html` 顺序复刻加载契约的**唯一低成本手段**，**不可替代**
- **必须补真实浏览器**：只有渲染引擎能把维度二的盲区变成断言

**建议五层**：
`L0 沙盒逻辑/契约`（保留并扩张，进 PR 门禁）→ `L1 静态分析(ESLint)` →
`L2 Playwright 关键路径 E2E` → `L3 axe 无障碍扫描` → `L4 视觉回归(可选)` →
`L5 构建/发布预检`（沿用 check-dist / verify-update-chain / launcher）

## 6.4 缺失工程化配置

| 项 | 解决什么 | 缺失后果 | 引入顺序 |
|---|---|---|---|
| ESLint | 跨文件未定义全局、未用变量、危险模式 | 无打包器下跨文件符号拼错**只能靠人眼** | 1（需可用 npm） |
| Prettier / EditorConfig | 统一格式 | 多文件搬迁必冲突、diff 噪声淹没真实改动 | 1–2 |
| Playwright E2E | 真实浏览器关键路径 | 维度二盲区永远测不到 | 2（同装 axe） |
| axe-core | aria/role/焦点/对比度 | 454 全绿仍 a11y 归零 | 2 |
| JSDoc + `tsc --checkJs` | 跨模块契约 | 重构静默破坏接口 | 3（拆模块后） |
| CHANGELOG / 版本策略 | 用户可见变更追溯 | 只有逐版发布说明 | 3 |
| vitest（可选） | 替代自制 reporter | — | 4（ESM 落地后，别先动） |

> ⚠️ **环境约束**：当前终端 `npm` 不可用（`WinError 2`）、`agent-browser` 亦不可用
> ⇒ 上述工具**必须在可用终端引入**，且当前无法落地浏览器自动化。
> 这直接决定了改进清单里"不依赖 npm"的项要排在前面。

## 6.5 文档缺口

现有 9 篇根目录 `.md`：4 篇面向用户（README / LOCAL_MODEL_README / PWA_INSTALL_GUIDE / MULTIMODAL_AND_STYLES）
+ 4 篇面向过去（两版 CODE_REVIEW、PROJECT_AUDIT_REPORT、TEMPLATE_TEST_REPORT）+ 本分析。

**缺面向未来/协作的**：
| 文档 | 作用 |
|---|---|
| `ARCHITECTURE.md` | 模块边界、数据流、扩展点、全局词法绑定契约 —— **兼作迁移设计稿（写不出来 = 边界没想清）** |
| `CONTRIBUTING.md` | 如何新增框架/模板/版式，**删文件四处同步清单**，如何跑校验（含 npm 环境坑） |
| `TESTING.md` | 12 套件各自覆盖什么/**测不到什么**/如何新增套件/负向测试协议 |
| `CHANGELOG.md` | 累积式用户可见变更 |
| `docs/adr/` | 记录"为何无打包器 / 为何经典脚本 / 为何自制沙盒"，**防止反复推翻决策** |
| `MIGRATION.md` | 迁移步骤 + 每步验收标准 |

---

# 七、模块化迁移方案（架构师核心产出）

## 7.1 推荐目录结构

```
js/
  core/                 # 技术基座（无业务语义，可被任何领域依赖）
    events.js           #   出口: PF.core.bus   —— 极简 pub/sub
    store.js            #   出口: PF.core.store（state + 只读订阅）
    dom.js              #   出口: PF.core.dom  （$ / escapeHTML / scheduleWrite）
    errors.js           #   出口: PF.core.errors（onerror + unhandledrejection + 环形日志）
    i18n.js             #   出口: PF.core.t(locale) —— 词条字典 + 插值
  data/                 # 纯数据资产（无副作用，可被任意依赖）
    frameworks.js       #   FRAMEWORKS / FRAMEWORK_DIMENSIONS（模型指令）
    templates.js        #   TEMPLATES / CATEGORIES
    presets.js          #   API_PRESETS
    i18n/zh-CN.js en-US.js   # UI 词条（与其余 data 隔离，避免"指令被误当词条"）
  domains/              # 按领域切，每域一个出口，禁止跨域读对方内部
    prompt-engine/index.js   # 追问/结构化/评分/对比    → PF.engine.*
    llm/index.js             # 统一 LLM 入口 + 在线/内置/离线路由 → PF.llm.*
    offline/index.js         # 离线模型管理 + 设备探测   → PF.offline.*
    persistence/index.js     # localStorage + settings schema/迁移 + 同步 → PF.db.*
    personalization/index.js # 主题/字体/背景/明暗      → PF.ui.theme.*
    export/index.js          # MD/JSON/PDF/分享/二维码   → PF.export.*
    views/index.js           # 各视图渲染              → PF.views.*
    platform/                # PWA + Tauri（更新/卸载/快捷方式）→ PF.platform.*
  app.js                # 仅引导：装配模块、绑事件、DOMContentLoaded（目标 < 300 行）
```

**公开出口约定**：每个模块**只**通过一个 `PF.<域>` 命名空间暴露；禁止读取别的域的私有变量；
旧全局名在迁移期由**垫片继续挂 `window`**，保证行为不变。

## 7.2 边界划分：按领域，而非技术分层

理由：
1. 本项目无后端、三形态共用源码，技术分层（model/view/controller）会把"提示词生成"这一内聚流程**切碎**，制造无收益的跨层跳转
2. 现有耦合是"全局状态 + DOM id"，按领域切能一次性把"同一业务的数据与视图"收进一个出口，**减少全局可写面**
3. 领域间依赖基本单向（`engine → llm → offline/persistence`；`views → engine/export`；`platform → persistence`），**可做到无环**

仅保留 `core/` 一层薄技术基座作为公共下界。

## 7.3 迁移路径：**不要一次性切 `type="module"`**

**硬约束**：`_check-lib.mjs` 用 `node:vm` 复刻经典脚本全局词法绑定，454 断言里的**行为型断言全部依赖它**；
`check-ui-wiring.mjs` 硬编码"17 个脚本 + 固定顺序"；`sync-dist` 与 `check-dist` 对 `js/` 做**非递归**计数。
一次性切模块 = **同时击穿测试/缓存/打包三条链路**，风险集中且不可回滚。

**分 4 步，前 3 步不依赖打包器：**

| 步骤 | 内容 | 验收标准 |
|---|---|---|
| **步骤 0 — 护栏**（不改结构） | 新增 `scripts/gen-depgraph.mjs` 扫描 `window.X=` 与顶层 const 跨文件引用，产出依赖图并断言**无环**；把 `index.html` 顺序约束落成断言；补全局错误兜底 | 依赖图可生成、无环；`check:all` 全绿且**断言数 ≥ 454** |
| **步骤 1 — 抽共享内核** | 建 `js/core/*`（events/store/dom/errors/i18n），**仍是经典脚本加载**；把 toast/escapeHTML/scheduleWrite/state 收进去，经 `PF.core` 暴露 + 旧名垫片 | `check-ui-wiring` 的脚本数量断言改为"== 从 index.html 解析出的数量"（**去掉硬编码 17**）；全套件通过；手工冒烟（优化/对比/导出/同步/更新）零回归 |
| **步骤 2 — 拆 `app.js`**（1814 → < 300 行） | 按 7.1 把 9 类职责拆到 `domains/`，**仍是经典脚本**，每文件单一职责、只暴露 `PF.<域>` | `app.js` 只剩装配与事件；依赖图无环；454 断言全绿；`jsFiles()` 改为**递归**以覆盖子目录 |
| **步骤 3 — 引入 Vite**（唯一不可逆） | 改 ESM + 打包；三处产物对齐（web=`dist` / tauri=`dist-app` / 单文件=内联） | 三形态功能对等；`check-dist` + `npm run release` 全链路通过；`sw.js` 清单**由构建生成**而非手写 |

## 7.4 风险控制

| 风险 | 控制手段 |
|---|---|
| 测试因结构变化失效 | 改 `_check-lib.mjs`：`ALL_SCRIPTS` 改为从 `index.html` 解析；`jsFiles()` 改递归；`check-ui-wiring` 去掉硬编码 17 |
| 旧全局名被 `PF.*` 取代导致断链 | 每个模块顶部加**兼容垫片**，迁移期同名 `window` 全局继续存在 |
| 一次搬太多 | **一次只搬一个领域**，每搬完跑 `check:all` + 5 项手工冒烟（离线生成/云同步/导出/更新/分享） |
| 离线缓存与源码漂移 | 每步都跑 `sync:dist`；新增文件自动纳入 `sw.js` 清单校验 |
| 打包引入后行为漂移 | 步骤 3 前置一次"经典脚本全绿"**快照**，作为功能对等基线 |

## 7.5 依赖关系：什么必须等模块化

**必须等（前置依赖 = 显式 import 边界）**：
类型安全（JSDoc + `tsc --checkJs`）、代码分割/懒加载、模块级单测（vitest）、tree-shaking、依赖注入。
> 在经典脚本 + 全局绑定下做这些只会得到"假模块"。

**不依赖模块化、可立刻做**：
全局错误兜底、防抖/节流、请求超时+重试、配置 schema + 版本迁移、
UI 文案抽 i18n 骨架（提示词资产不动）、轻量可观测性（环形日志 + 上报钩子）、
`state` 显式字段声明、接入 ESLint/Prettier。

---

# 八、迁移期质量门禁（QA 核心产出）

## 8.1 如何不"搬一处崩一片"

1. **先冻边界再搬内部** —— 每模块先声明导出清单，加"边界契约"套件锁死导出名，内部搬迁无法静默改接口
2. **行为基线金快照** —— 迁移前 `loadAll` 捕获全局绑定集合 + `buildPrompt`/版式输出的 fixture 基线，每步必须复现（证明是**保行为重构**）
3. **迁移期坚持经典脚本加载**，不提前切 ESM，`check-ui-wiring` 顺序断言全程有效
4. **一模块一 PR**：搬 → `run-all-checks` → 补该模块套件 → 再下一步，**禁批量搬**

## 8.2 每步验收标准（须全部满足）
- `run-all-checks` 全绿且**断言数不降**
- 新模块自带套件且**含校验器自检**
- 边界契约套件通过；金快照无差异
- 无新增幽灵 id / 未声明全局
- 脚本序若变动须**同 PR** 更新 `check-ui-wiring`
- `npm run release` 端到端仍绿
- diff 只碰单一边界

## 8.3 门禁清单

| 级别 | 条件 |
|---|---|
| **禁合并** | `run-all-checks` 红；新模块无套件；改边界未同步契约文件与调用方；新增未声明全局；SW 清单/脚本不一致；ESLint error；金快照差异未解释；（L3 落地后）a11y 严重级别回退 |
| **禁发布** | `check:dist` 红；`verify:update` 不足 7 场景；launcher 自测非 22/22；`version.json` 未 bump；mtime 新鲜度失败；存在任何 `FAIL` 行 |
| **仅告警** | 体积增长超阈值 |

## 8.4 新套件命名与职责
`.workbuddy/check-<domain>.mjs`，头注释必写"**为何单列 + 历史教训**"。

| 套件 | 职责 |
|---|---|
| `check-module-boundaries.mjs` | 断言各模块导出名存在且**仅泄漏这些全局**；越权访问未导出名即失败 |
| `check-behavior-baseline.mjs` | 金快照比对 |
| **`check-a11y-static.mjs`** | **低成本把维度二盲区变成断言**：图标按钮有 `aria-label`、弹窗有 `role`、存在 `focus-visible`/`reduced-motion`、无原生 `confirm()`、关键容器有空状态、输入类 handler 有防抖。**npm 坏了也能跑** |
| 拆分后 | `check-history.mjs` / `check-share.mjs` / `check-settings-schema.mjs` |
| auto-gen 后 | `check-scripts-manifest.mjs`（生成的 SW 清单 == 构建产物） |

## 8.5 杜绝"断言恒真"（制度化负向测试）
1. **强制校验器自检**：每个新套件含"自检"段，由元套件 `check-suites-selfcheck.mjs` grep 标记并验证 `fail()` 确实计失效、退出码非 0
2. **变异测试**：`check-mutation.mjs` 向临时副本注入已知缺陷（改名/删行），断言对应套件**转红**；维护变异库，PR 跑子集、每周跑全量
   —— 直接复刻"只搜 `PromptForge-${V}-Setup.exe` 导致断言恒真"的教训
3. **禁止"只凭 `src.includes(裸名)`"的断言**，须锚定到语句行，元套件自动标记
4. **断言数下限**（套件级 + 全局），防"删断言求绿"

---

# 九、改进清单（按投入产出比排序）

> 工作量按"1 名熟悉本项目的开发者"估算，单位 **人天**。
> **P0** = 不依赖模块化、不依赖 npm，可立刻做（止血）
> **P1** = 与模块化并行（护栏 + 迁移）
> **P2** = 模块化完成后

## P0：立刻做（合计约 12–16 人天）

| # | 改进项 | 解决什么 | 前置依赖 | 工作量 | 风险 |
|---|---|---|---|---|---|
| 1 | **`prefers-reduced-motion` 适配** | 维度2 无障碍硬伤 | 无 | **0.5** | 极低 |
| 2 | **`:focus-visible` 全局样式** | 键盘用户看不到焦点 | 无 | **0.5** | 极低 |
| 3 | **全局错误兜底**（`onerror` + `unhandledrejection` + 友好提示） | 静默白屏、无诊断信息 | 无 | **1** | 极低 |
| 4 | **输入类防抖/节流**（250ms） | 输入诊断/resize/轮询裸奔 | 无 | **1** | 低 |
| 5 | **Toast 升级**：4 类型 + 队列 + `aria-live` + 时长自适应 | 反馈覆盖/丢失/读屏不播报 | 无 | **1** | 低 |
| 6 | **Dialog 统一**：`askDialog` 加 `role`/`aria-modal`/焦点陷阱/Esc/还焦；**替换 4 处原生 `confirm()`** | 弹窗风格割裂 + 键盘功能性阻断 | 无 | **1.5** | 低 |
| 7 | **`check-a11y-static.mjs`**（沙盒静态 a11y 护栏） | 把维度二盲区变成断言，**npm 坏了也能跑** | 无 | **1.5** | 低 |
| 8 | **空状态补齐**（模板筛选无结果 / 对比未输入 / 模型未下载） | 列表裸白屏 | 无 | **2** | 低 |
| 9 | **`label for` 绑定 + 导航 `aria-current`** | 表单与导航可访问性 | 无 | **1** | 低 |
| 10 | **骨架屏**（历史/模板/设置） | 切视图闪白 | 无 | **1.5** | 低 |
| 11 | **`state` 显式字段声明** + 轻量可观测性（环形日志 + 上报钩子） | 隐式字段、无复现线索 | 无 | **1** | 低 |
| 12 | **`scripts/gen-depgraph.mjs`** + 无环断言 | 顺序耦合不可见 | 无 | **1** | 低 |

> P0 做完的即时效果：**从"能用"变成"用起来不别扭"，且合规红线补齐**。

## P1：与模块化并行（合计约 19–25 人天）

| # | 改进项 | 解决什么 | 前置依赖 | 工作量 | 风险 |
|---|---|---|---|---|---|
| 13 | **ESLint + Prettier + EditorConfig** | 跨文件符号拼错只能靠人眼 | 可用 npm | **1** | 低 |
| 14 | **`ARCHITECTURE.md` + `TESTING.md` + `CONTRIBUTING.md`** | 缺迁移设计稿与协作文档 | 无 | **2** | 低 |
| 15 | **配置 schema + 版本迁移链** | 改字段名会静默错乱老用户 | 无 | **1.5** | 低 |
| 16 | **步骤 1：抽 `js/core/*`** | 状态/DOM/事件无归属 | #12 | **3** | 中 |
| 17 | **步骤 2：拆 `app.js`**（1814 → < 300 行） | 单文件 9 职责 | #16 | **5** | 中高 |
| 18 | **模块边界契约 + 金快照套件** | 搬迁静默改接口/改行为 | #16 | **2** | 低 |
| 19 | **元套件自检 + 变异测试库** | 断言恒真的假绿 | #18 | **2** | 低 |
| 20 | **Playwright E2E + axe 扫描** | 真实渲染/无障碍盲区 | 可用 npm + 浏览器 | **4** | 中 |

## P2：模块化完成后（合计约 12–18 人天）

| # | 改进项 | 解决什么 | 前置依赖 | 工作量 | 风险 |
|---|---|---|---|---|---|
| 21 | **UI 文案 i18n**（约 500 处；150 处指令语料另作 prompt pack） | 794 处硬编码出血点 | #17（必须） | **3** | 中 |
| 22 | **乐观更新 + Undo** | 破坏性操作只有拦截式确认 | #17 | **2.5** | 中 |
| 23 | **统一 retry/backoff + 错误边界** | 网络失败只能手动重试 | #17 | **1.5** | 中 |
| 24 | **步骤 3：引入 Vite + 三形态对齐** | 无法懒加载/类型化 | #17 全绿 | **6** | **高（不可逆）** |
| 25 | **类型安全**（JSDoc 渐进 + `tsc --checkJs`） | 重构静默破坏接口 | #24 | **4** | 中 |
| 26 | **代码分割 / 按需加载**（离线模型、导出 PDF、帮助页） | 首屏全量加载 | #24 | **3** | 中 |
| 27 | **快捷键体系**（Esc 返回、Ctrl+Enter 提交） | 效率用户 | #6 #24 | **2** | 中 |
| 28 | **CHANGELOG + `docs/adr/`** | 用户不可见版本变化、决策被反复推翻 | 无 | **1** | 低 |
| 29 | **行为可定制**（默认框架/语言/版式、导出模板） | 可定制偏外观 | #15 | **2** | 低 |

## 9.1 三条硬约束（务必遵守）

1. **模块化必须与校验同步推进** —— 现有 454 项断言是唯一的回归网。
   每拆一个模块立即跑 `run-all-checks`，并**为拆出来的模块补一个套件**。
   本项目历史上的灾难（9 个校验脚本丢失、`callLLM` 递归覆盖）都源于"改了但没校验"。

2. **i18n 不要提前做** —— 794 处字符串在模块边界未定时提取 = 白做一遍。必须等 #17 完成。

3. **不要一次性切 `<script type="module">`** —— 先"目录模块化 + 保持经典脚本加载"，稳定后再切 ESM（#24 是唯一不可逆步骤）。

## 9.2 关键路径（建议节奏）

```
第 1 周   P0 #1–#4、#6、#7      → 无障碍红线 + 键盘可用 + 崩溃可见（风险极低）
第 1 周   P0 #5、#8、#9、#10    → 反馈体系 + 空状态 + 骨架屏
第 1 周   P0 #11、#12           → 可观测 + 依赖图（为迁移铺路）
          ↓ 产品从"能用"变成"用起来不别扭"

第 2 周   #13、#14、#15         → 工具链与文档就位
第 3–4 周 #16 → #18 → #17       → 抽内核 → 建边界 → 拆 app.js（一模块一 PR）
第 4–5 周 #19、#20              → 变异测试 + 真实浏览器层
          ↓ 新增功能不再需要动 app.js

第 6 周起 #21–#23 → #24 → #25–#29
          ↓ 打包器（不可逆）与类型化、i18n、懒加载
```

---

# 十、附：实测依据

| 指标 | 数值 | 来源 |
|---|---|---|
| JS 文件数 / 总行数 | 17 / 约 3600 行 | `js/` 扫描 |
| `app.js` | 1814 行 / 70KB / 顶层 79 个函数 | 文件统计 |
| 显式 `window.X =` 补挂 | 26 处 | 全 `js/` 扫描 |
| `getElementById` 调用 | 60+ 处 | 全 `js/` 扫描 |
| CSS 变量 / keyframes / transition / media | 38 / 6 / 24 / 10 | `css/style.css` |
| `prefers-reduced-motion` / `:focus-visible` | **0 / 0** | `css/style.css` |
| `aria-*` / `role` / `tabindex`（index.html） | **1 / 0 / 0** | `index.html` |
| 语义 `<button>` / `div onclick` | 95 / 0 | `index.html` |
| `<label>` / `label for=` | 19 / **0** | `index.html` |
| 键盘事件处理（全 `js/`） | **1** | `app.js:1785` |
| 原生 `confirm()` | **4 处** | `app.js:945`、`export.js:78`、`local-model.js:486`、`offline-ui.js:182` |
| `askDialog` 调用点 | 6 次 | `app.js:1139` 定义 |
| Toast 能力 | 单元素 / 固定 2500ms / 无类型 / 无队列 / 无 aria-live | `app.js:1086` |
| 空状态 | **1 处** | `app.js:907` |
| 骨架屏 | 1 处 | `app.js:289` |
| 全局错误兜底 | **0** | 全 `js/` 扫描 |
| 防抖/节流 / 懒加载 | **0 / 0** | 全 `js/` 扫描 |
| 含中文字符串字面量 | **794** | 全 `js/` 扫描 |
| 自动化断言 | 454（12 套件） | `run-all-checks` |
| 工程化配置（lint/format/ts/E2E） | **全 0** | 根目录扫描 |
| `.md` 文档 | 9 篇（4 篇面向用户、4 篇面向过去、1 篇本分析） | 根目录扫描 |
| `dependencies` / `devDependencies` | 空 / 仅 `@tauri-apps/cli` | `package.json` |
